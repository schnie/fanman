/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/**
 * The build this bundle was made from, stamped in by `define` in
 * `vite.config.ts`. Settings shows it verbatim, because an installed
 * home-screen app gives you no other way to tell which code you are running —
 * and "is it even updating?" is unanswerable without that.
 */
declare const __APP_BUILD__: string
